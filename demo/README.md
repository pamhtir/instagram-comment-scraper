# Demo assets

`sample_comments.csv` and `sample_output.xlsx` use synthetic identities and text. They demonstrate the delivery format, not live scraping results. Replace the placeholder files in `assets/` with a real redacted GIF and screenshot only after the manual acceptance matrix passes.
